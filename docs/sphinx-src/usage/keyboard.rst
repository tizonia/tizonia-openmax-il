Keyboard Shortcuts
==================

TODO

.. code-block:: bash

   tizonia --help keyboard

   Keyboard control:

      [p] skip to previous file.
      [n] skip to next file.
      [SPACE] pause playback.
      [+/-] increase/decrease volume.
      [m] mute.
      [q] quit.
