vpx_dep = declare_dependency(
   include_directories: include_directories(join_paths(get_option('prefix'), get_option('includedir'), 'vpx')),
   dependencies: dependency('vpx', required: true)
)

subdir('src')
